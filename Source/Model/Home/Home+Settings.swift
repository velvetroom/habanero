import Foundation

extension Home
{
    //MARK: private
    
    private func asyncLoadSettings(completion:@escaping(() -> ()))
    {
        
    }
    
    //MARK: internal
    
    func loadSettings(completion:@escaping(() -> ()))
    {
        
    }
}
